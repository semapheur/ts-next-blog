---
title: 'Hartree-Fock Theory'
subject: 'Physics'
showToc: true
references:
  - book_shavitt_bartlett_2009
  - book_szabo_ostlund_1996
---

# The Hartree-Fock equations

Let $\mathbf{x} = (\mathbf{r}, \omega)$ denote combined spatial and spin coordinates. With the Hartree-Fock method, we seek a set of orthonormal spin orbitals $\set{\chi_i}_{i=1}^N$ with $\braket{\chi_i|\chi_j} = \delta_{ij}$, such that the corresponding Slater determinant

$$
  \Psi_0 = \frac{1}{\sqrt{N!}} \det[\chi_i (\mathbf{x}_j)]
$$

provides the best approximation to the exact ground-state wavefunction of an $N$-electron system described by the electronic Hamiltonian $\hat{H}$.

According to the variational principle, the optimal spin orbitals minimize the Hartree-Fock energy functional

$$
\begin{align*}
  E[\set{\chi_i}] =& \braket{\Psi_0|\hat{H}|\Psi_0} \\
  =& \sum_{i=1}^N \braket{i|\hat{h}|i} + \frac{1}{2} \sum_{i,j=1}^N \bra{ij}\ket{ij}
\end{align*}
$$

subject to the orthonormality constraints on the spin orbitals. Using Lagrange multipliers, minimization of the energy functional under the orthonormality constraints gives the Hartree-Fock equation

$$
\begin{equation*}
  \hat{h}(1) \chi_i(1) + \sum_{j=1}^N [\hat{J}_j (1) - \hat{K}_j (1)] \chi_i (1) = \varepsilon_i \chi_i (1)
\tag{\label{equation-1}}
\end{equation*}
$$

where $\varepsilon_i$ is the Lagrange multiplier, called the orbital energy, associated with spin orbital $\chi_i$.

**Coulomb operator**

The Coulomb operator associated with the occupied spin orbital $\chi_j$ is defined by

$$
\begin{equation*}
  (\hat{J}_j \phi)(1) = \left[\int \frac{|\chi_j (2)|^2}{r_{12}} \;\d\mathbf{x}_2 \right] \phi(1)
\tag{\label{equation-3}}
\end{equation*}
$$

for an arbitrary spin orbital $\phi$. Thus, $\hat{J}_j$ is a local multiplicative operator whose value at $\mathbf{x}_1$ depends only on the electron density associated with orbital $\chi_j$. It represents the average electrostatic potential generated by the probability density $|\chi_j|^2$ of electron $2$. Summing over all occupied orbitals therefore gives the mean electrostatic potential experience by an electron occupying orbital $\chi_i$.

The expectation value of $\hat{J}_j$ is given by the Coulomb integral

$$
  \braket{\chi_i|\hat{J}_j|\chi_i} = J_{ij} = [ii|jj]
$$

**Exchange operator**

The exchange operator associated with the occupied spin orbital $\chi_j$ is defined by

$$
\begin{equation*}
  (\hat{K}_j \phi)(1) = \left[\int \chi_j^* (2) \frac{1}{r_{12}} \phi(2) \;\d\mathbf{x}_2 \right] \chi_j (1)
\tag{\label{equation-4}}
\end{equation*}
$$

Unlike the Coulomb operator, $\hat{K}_j$ is nonlocal, because its action at the point $\mathbf{x}_1$ depends on the values of the function $\phi$ throughout all space. It has no classical analogue, because it arises solely from the antisymmetry of the Slater determinant and therefore reflects the fermionic nature of the electronic wavefunction. Its nonlocal character is a direct consequence of electron exchange. 

The expectation value of $\hat{K}_j$ is given by the exchange integral

$$
  \braket{\chi_i|\hat{K}_j|\chi_i} = K_{ij} = [ij|ji]
$$

**Fock operator**

Since $(\hat{J}_i - \hat{K}_i)\chi_i = 0$, the self-interaction terms cancel identically. The summation in $\eqref{equation-1}$ can therefore be taken over all occupied orbital, allowing the introduction of the Fock operator

$$
\begin{equation*}
  \hat{f} := \hat{h} + \sum_{j=1}^N (\hat{J}_j - \hat{K}_j)
\tag{\label{equation-5}}
\end{equation*}
$$

which depends parametrically on the occupied spin orbitals. In terms of the Fock operator, the Hartree-Fock equations assume the compact form

$$
\begin{equation*}
  \hat{f} \chi_i = \varepsilon_i \chi_i
\tag{\label{equation-6}}
\end{equation*}
$$

The effective one-electron potential

$$
  \hat{v}_\text{HF} = \sum_{j=1}^N (\hat{J}_j - \hat{K}_j)
$$

is known as the Hartree-Fock potential, so that 

$$
\begin{equation*}
  \hat{f} = \hat{h} + \hat{v}_\text{HF}
\tag{\label{equation-16}}
\end{equation*}
$$

The exchange operator can also be expressed using the electron-permutation operator $\hat{p}_{12}$, defined by $\hat{p}_{12} v(1,2) = v(2,1)$. With this notation

$$
\begin{equation*}
  \hat{f}(1) = \hat{h}(1) + \sum_{j=1}^N \int \chi_j^* (2) \frac{1 - \hat{p}_{12}}{r_{12}} \chi_j (2) \;\d\mathbf{x}_2
\tag{\label{equation-18}}
\end{equation*}
$$

Although the Hartree-Fock equations resemble a one-electron eigenvalue problem, they do not constitute a linear eigenvalue problem in the usual sense. The Fock operator depends functionally on the occupied orbitals through the Coulomb and exchange operators, i.e. $\hat{f} = \hat{f}[\set{\chi_i}]$, so the orbitals sought determine the operator itself. The Hartree-Fock equations therefore define a nonlinear, self-consisten eigenvalue problem.

Except for atoms and highly symmetric systems, the Hartree-Fock equations cannot be solved analytically. Instead, each spin orbital is expanded in a finite basis set, resulting the Roothan-Hall or Pople-Nesbet matrix equations, which are solved interatively until self-consistency is achieved. As the basis set approaches completeness, the resulting orbitals converge to the Hartree-Fock limint, providing the exact solution within the space of all single Slater determinants.

## Derivation of the Hartree-Fock equations

### Functional variation

Let $\tilde{\Phi}$ be a trial wavefunction in the domain of the electronic Hamiltonian $\hat{H}$. The corresponding energy is define by the Rayleigh quotient

$$
  E[\tilde{\Phi}] = \frac{\braket{\tilde{\Phi}|\hat{H}|\tilde{\Phi}}}{\braket{\tilde{\Phi}|\tilde{\Phi}}}
$$

Thus, $E[\tilde{\Phi}]$ is a functional: its value depends on the entire function $\tilde{\Phi}$, rather than on the value of an independent variable. If the trial wavefunction is normalized, $\braket{\tilde{\Phi}|\tilde{\Phi}}$, the energy functional reduces to

$$
  E[\tilde{\Phi}] = \braket{\tilde{\Phi}|\hat{H}|\tilde{\Phi}}
$$

Suppose $\tilde{\Phi}$ is subjected to an arbitrary infinitesimal variation

$$
  \tilde{\Phi} \to \tilde{\Phi} + \delta\tilde{\Phi}
$$

where $\delta\tilde{\Phi}$ is an arbitrary admissible variation. Expanding the energy functional to first order gives

$$
\begin{align*}
  E[\tilde{\Phi} + \delta\tilde{\Phi}] =& \braket{\tilde{\Phi} + \delta\tilde{\Phi}|\hat{H}|\tilde{\Phi} + \delta{\Phi}} + O(\delta\tilde{\Phi}^2) \\
  =& E[\tilde{\Phi}] + \braket{\delta\tilde{\Phi}|\hat{H}|\tilde{\Phi}} + \braket{\tilde{\Phi}|\hat{H}|\delta\tilde{\Phi}} + O(\delta\tilde{\Phi}^2) \\
\end{align*}
$$

The first-order term, $\delta E = \braket{\delta\tilde{\Phi}|\hat{H}|\tilde{\Phi}} + \braket{\tilde{\Phi}|\hat{H}|\delta\tilde{\Phi}}$, is called the first variation of the energy functional.

The variational principle states that the optimal trial wavefunction is obtained by requiring the energy functional to be stationary with respect to all admissible variations, $\delta E = 0$. This condition is neccessary for an extremum of the functional. For the ground state of a Hamiltonian bounded from below, the variational principle guarantees that this stationary point is in fact the global minimum.

Consider the linear variational ansatz

$$
  \ket{\tilde{\Phi}} = \sum_{i=1}^N c_i \ket{\Psi_i}
$$

where the basis functions $\set{\ket{\Psi_i}}$ are linearly independent but not necessarily orthonormal. The normalization condition is

$$
  \braket{\tilde{\Phi}|\tilde{\Phi}} = \sum_{i,j} c_i^* S_{ij} c_j = 1
$$

where $S_{ij} = \braket{\Psi_i|\Psi_j}$ is the overlap matrix. Similarly,

$$
  \braket{\tilde{\Phi}|\hat{H}|\tilde{\Phi}} = \sum_{i,j} c_i^* H_{ij} c_j
$$

where $H_{ij} = \braket{\Psi_i|\hat{H}|\Psi_i}$ are the Hamiltonian matrix elements.

To impose the normalization constraint, we introduce the Lagrangian

$$
\begin{align*}
  L =& \braket{\tilde{\Phi}|\hat{H}|\tilde{\Phi}} - E(\braket{\tilde{\Phi}} - 1) \\
  =& \sum_{i,j} c_i^* H_{ij} c_j - E\left(\sum_{i,j} c_î^* c_j S_{ij} - 1 \right)
\end{align*}
$$

where the Lagrangian multiplier $E$ is identified with the variational energy

The coefficients $c_i$ and $c_i^*$ are treated as independent variable. Taking the first variation of $L$ with respect to $c_i^*$ gives

$$
\begin{align*}
  \delta L =& \sum_{i,j} \delta c_i^* H_{ij} c_j - E \sum_{i,j} \delta c_i^* S_{ij} c_j \\
  &+ \sum_{i,j} c_i^* H_{ij} \delta c_j - E \sum_{i,j} c_i^* S_{ij} \delta c_i
\end{align*}
$$

Since $E$ is real, after collecting terms and interchanging indices, we get

$$
  \delta L = \sum_i \delta c_i^* \left[\sum_j H_{ij} c_j - E \sum_j S_{ij} c_j \right] + \text{c.c.}
$$

Since the variations $\set{\delta c_i^*}$ are arbitrary, stationarity $\delta L = 0$ requires

$$
  \sum_{j} H_{ij} c_j = E \sum_j S_{ij} c_j
$$

or in matrix form $\mathbf{Hc} = E\mathbf{Sc}$, which is the generalized Hermitian eigenvalue problem arising in the linear variation method. If the basis is orthonormal, then $\mathbf{S} = \mathbf{I}$, and the equation reduces to the ordinary eigenvalue problem $\mathbf{Hc} = E\mathbf{c}$. The smallest eigenvalue of the generalized eigenvalue problem provides the best approximation to the ground-state energy within the span of the chosen basis functions, while the corresponding eigenvector determines the optimal linear combination of basis functions.

### Minimization of the energy of a single determinant

Given a single Slater determinant $\ket{\Psi_0} = \ket{\chi_1 \cdots \chi_N}$, the expectation value $E_0 = \braket{\Psi_0|\hat{H}|\Psi_0}$ defines a functional of the occupied spin orbitals $\set{\chi_i}$. The Hartree-Fock equations are obtained by minimizing this functional over all orthonormal sets of spin orbitals satisfying

$$
  \braket{i|j} := \int \chi_i^* (1) \chi_j \;\d\mathbf{x}_1 = \delta_{ij}
$$

This orthonormality conditions produces the constraints $\braket{i|j} - \delta_{ij} = 0$. To enforce these constraints, we introduce the Lagrangian functional

$$
  L[\set{\chi_i}] = E_0 [\set{\chi_i}] - \sum_{i=1}^N \sum_{j=1}^N \varepsilon_{ji} (\braket{i|j} - \delta_{ij})
$$

where the matrix $\boldsymbol{\varepsilon} = [\varepsilon_{ij}]$ consists of Lagrange multipliers.

where $E_0$ is the expectation value of the single determinant $\ket{\Psi_0}$.

$$
\begin{equation*}
  E_0 [\set{\chi_i}] = \sum_{i=1}^N \braket{i|\hat{h}|i} + \frac{1}{2} \sum_{i=1}^N \sum_{j=1}^N [ii|jj] - [ij|ji]
\tag{\label{equation-2}}
\end{equation*}
$$

Because $L$ is real-valued and $\braket{i|j} = \braket{j|i}^*$, the matrix of Lagrange multipliers must be Hermitian, i.e. $\varepsilon_{ji} = \varepsilon_{ij}^*$.

Minimization of $E_0$, subject to the orthonormality constraints, is thus obtained by solving $\delta L = 0$. Treating the spin orbitals and their complex conjugates as independent variables, we consider arbitrary infinitesimal variations

$$
\begin{align*}
  \chi_i \to& \chi_i + \delta\chi_i \\
  \chi_i^* \to& \chi_i^* + \delta\chi_i^*
\end{align*}
$$

Taking the first variation of $\eqref{equation-2}$, we impose

$$
  \delta L = \delta E_0 - \sum_{i=1}^N \sum_{j=1}^N \varepsilon_{ji} \delta\braket{i|j} = 0
$$

where have used that $\delta(\delta_{ij}) = 0$. Taking the first variation of the spin orbital overlaps

$$
  \delta\braket{i|j} = \braket{\delta\chi_i|\chi_j} + \braket{\chi_i|\delta\chi_j}
$$

and using the Hermiticity of $\boldsymbol{\varepsilon}$, we obtain

$$
\begin{align*}
  \sum_{i,j} \varepsilon_{ji} (\braket{\delta\chi_i|\chi_j} + \braket{\chi_i|\delta\chi_j}) =& \sum_{i,j} \varepsilon_{ji} \braket{\delta\chi_i|\chi_j} + \sum_{i,j} \varepsilon_{ij} \braket{\chi_j|\delta\chi_i} \\
  =& \sum_{i,j} \varepsilon_{ji} \braket{\delta\chi_i|\chi_j} + \sum_{i,j} \varepsilon_{ji}^* \braket{\delta\chi_i|\chi_j} \\
  =& 2 \Re \left(\sum_{i,j} \varepsilon_{ji} \braket{\delta\chi_i|\chi_j} \right)
\end{align*}
$$

The first variation of the energy is

$$
\begin{align*}
  \delta E_0 =& \sum_{i=1}^N \braket{\delta\chi_i|\hat{h}|\chi_i} + \braket{\chi_i|\hat{h}|\chi_i} \\
  &+ \frac{1}{2} \sum_{i=1}^N \sum_{j=1}^N [\delta\chi_i \chi_i|\chi_j \chi_j] + [\chi_i \delta\chi_i|\chi_j \chi_j] + [\chi_i \chi_i|\delta\chi_j \chi_j] + [\chi_i \chi_i|\chi_j \delta\chi_j] \\
  &- \frac{1}{2} \sum_{i=1}^N \sum_{j=1}^N [\delta\chi_i \chi_j|\chi_j \chi_i] + [\chi_i \delta\chi_j|\chi_j \chi_i] + [\chi_i \chi_j|\delta\chi_j \chi_i] + [\chi_i \chi_j|\chi_j \delta\chi_i]
\end{align*}
$$

Using the permutation symmetry of the two-electron integrals and relabelling dummy indices, this expression simplifies to

$$
\begin{align*}
  \delta E_0 =& \sum_{i=1} \braket{\delta\chi_i|\hat{h}|\chi_i} + \sum_{i=1}^N \sum_{j=1}^N [\delta\chi_i \chi_i|\chi_j \chi_j] - [\delta\chi_i \chi_j|\chi_j \chi_i] \\
  &+ \text{c.c.}
\end{align*}
$$

Combining these results, the first variation in $L$ becomes

$$
\begin{align*}
  \delta L =& \sum_{i=1}^N \braket{\delta\chi_i|\hat{h}|\chi_i} \\
  &+ \sum_{i=1}^N \sum_{j=1}^N [\delta\chi_i \chi_i|\chi_j \chi_j] - [\delta\chi_i \chi_j|\chi_j \chi_i] \\
  &- \sum_{i=1}^N \sum_{j=1}^N \varepsilon_{ji} \braket{\delta\chi_i|\chi_j} + \text{c.c.} = 0
\end{align*}
$$

Using definitions $\eqref{equation-3}$ and $\eqref{equation-4}$ for the Coulomb and exchange operators, respectively, we can write this variation in the form

$$
\begin{align*}
  \delta L =& \sum_{j=1}^N \int \delta\chi_i^* (1) \left(\hat{h}(1) + \sum_{j=1}^N [\hat{J}_j (1) - \hat{K}_j (1)] \right) \;\d\mathbf{x}_1 \\
  &- \sum_{i,j=1}^N \varepsilon_{ji} \int \delta\chi_i^* (1) \chi_j (1) \;\d\mathbf{x}_1 + \text{c.c} = 0
\end{align*}
$$

Since the variations $\delta\chi_i^* (1)$ are arbitrary, the fundamental lemma of variational calculus implies that

$$
  \left[\hat{h}(1) + \sum_{j=1}^N \hat{J}_j (1) - \hat{K}_j (1) \right] \chi_i (1) = \sum_{j=1}^N \varepsilon_{ji} \chi_j (1),\; j=1,\dots,N
$$

Recognizing the Fock operator $\eqref{equation-5}$, this equation takes the compact form

$$
\begin{equation*}
  \hat{f}\ket{\chi_i} = \sum_{j=1}^N \varepsilon_{j=1}^N \varepsilon_{ji} \ket{\chi_i}
\tag{\label{equation-7}}
\end{equation*}
$$

These equations are not yet in the canonical eigenvalue form of $\eqref{equation-6}$ because the occupied orbitals are determined only up to an arbitrary unitary transformation. Replacing the occupied spin orbitals by any unitary lienar combination leaves the Slater determinant unchanged up to an overall phase, and therefore leaves the Hartree-Fock energy invariant. Exploiting this unitary freedom, the Hermitian matrix $\boldsymbol{\varepsilon}$ can be diagonalized, resulting in $\eqref{equation-6}$.

### Canonical Hartree-Fock equations

Let $\mathbf{U} = [U_{ij}]$ be a unitary transformation satisfying 

$$
  \mathbf{U}^\dagger \mathbf{U} = \mathbf{UU}^\dagger = \mathbf{I}
$$

Consider a new set of occupied spin orbitals $\set{\chi'_i}_{i=1}^N$ transformed by $\mathbf{U}$ from the original set $\set{\chi_i}$

$$
\begin{equation*}
  \chi'_i (\mathbf{x}_j) = \sum_{k=1} \chi_k (\mathbf{x}_j) U_{ki}
\tag{\label{equation-9}}
\end{equation*}
$$

Defining the matrix $\mathbf{A} = [\chi_k (j)]$, this can be written as $\mathbf{A}' = \mathbf{AU}$.

The normalized Slater determinant constructed from the transformed orbitals is

$$
  \Psi' = \frac{1}{\sqrt{N!}} \det[\chi'_i (\mathbf{x}_j)] = \frac{1}{\sqrt{N!}} \det(\mathbf{AU})
$$

Using the multiplicative property of determinants

$$
  \det(\mathbf{AU}) = \det(\mathbf{A})\det(\mathbf{U})
$$

we obtain

$$
  \Psi' = \underbrace{\frac{1}{\sqrt{N!}} \det(\mathbf{A})}_{=\Psi}\det(\mathbf{U}) = \det(\mathbf{U}) \Psi
$$

By unitarity of $\mathbf{U}$, it follows that $|\det{\mathbf{U}}| = 1$ such that $\det(\mathbf{U}) = e^{i\phi}$ for some real phase $\phi\in\R$. Consequently, $\ket{\Psi'} = e^{i\phi} \ket{\Psi}$ showing that the two Slater determinants constructed from occupied spin orbitals related by a unitary transformation differ only by an overall phase. 

Since all expectation values depend on the projector $\ket{\Psi}\bra{\Psi}$, they are invariant under unitary transformations. That is $\braket{\Psi'|\hat{A}|\Psi'} = \braket{\Psi|\hat{A}|\Psi}$ for every observable $\hat{A}$. Consequently, the occupied orbitals that minimize the Hartree-Fock energy are not uniquely determined; only the occupied subspace has physical significance.

---

To obtain the canonical Hartree-Fock equations, we examine how the Fock operator and the matrix of Lagrange multipliers transform under orbital rotations.

The transformed total Coulomb operator under $\mathbf{U}$ is

$$
\begin{align*}
  \sum_i \hat{J}'_i (1) =& \sum_i \int {\chi'_i}^* (2) \frac{1}{r_{12}} \chi'_i (2) \;\d\mathbf{x}_2 \\
  =& \sum_{j,k} \left(\sum_i U_{ji}^* U_{ki} \right) \int \chi_j^* (2) \frac{1}{r_{12}} \chi_k (2) \;\d\mathbf{x}_2
\end{align*}
$$

Since

$$
  \sum_i U_{ji}^* U_{ki} = (\mathbf{U}\mathbf{U}^\dagger)_{kj} = \delta_{kj}
$$

it follows that

$$
\begin{align*}
  \sum_i \hat{J}'_i (1) =& \sum_j \int \chi_j^* (2) \frac{1}{r_{12}} \chi_j (2) \;\d\mathbf{x}_2 \\
  =& \sum_j \hat{J}_j (1)
\end{align*}
$$

showing that the total Coulomb operator is invariant under unitary transformations of the occupied spin orbitals. Similarly, for an arbitrary spin orbital $\varphi$, the transformed total exchange operator under $\mathbf{U}$ satisfies

$$
\begin{align*}
  \left(\sum_i \hat{K}'_i \right)\varphi =& \sum_i \left(\int {\chi'_i}^* (2) \frac{1}{r_{12}} \varphi(2) \;\d\mathbf{x}_2 \right)\chi'_i (1) \\
  =& \sum_{j,k} \underbrace{\left(\sum_i U_{ji}^* U_{ki} \right)}_{=\delta_{jk}} \left(\int \chi_j^* (2) \frac{1}{r_{12}} \varphi(2) \;\d\mathbf{x}_2 \right) \chi_k (1) \\
  =& \sum_j \left(\int \chi_j^* (2) \frac{1}{r_{12}} \varphi(2) \;\d\mathbf{x}_2 \right) \chi_j (1) \\
  =& \left(\sum_j \hat{K}_j \right) \varphi
\end{align*}
$$

Since this holds for every spin orbital $\varphi$, the total exchange operator is invariant under unitary transformations. 

These results implies that the Fock operator is invariant under arbitrary unitary transformations of the spin orbitals, i.e. $f' = f$.

It remains to determine the effect of the unitary transformation on the Lagrange multipliers. Taking the inner product of $\eqref{equation-7}$ with $\ket{\chi_k}$ yields the expectation value

$$
\begin{align*}
  \braket{\chi_k|\hat{f}|\chi_i} =& \sum_{j=1}^N \varepsilon_{ji} \braket{\chi_k|\chi_j} \\
  =& \varepsilon_{ki}
\end{align*} 
$$

so that the matrix of Lagrange multipliers is simply the representation of the Fock operator in the occupied orbital basis. The transformed Lagrange multipliers under $\mathbf{U}$ become

$$
\begin{align*}
  \varepsilon'_{ij} =& \int {\chi'_i}^*(1) \hat{f}(1) \chi'_j (1) \;\d\mathbf{x}_1 \\
  =& \sum_{k,l} U_{ki}^* U_{kj} \int \chi_k^* (1) \hat{f}(1) \chi_k (1) \;\d\mathbf{x}_1 \\
  =& \sum_{k,l} U_{ki}^* \varepsilon_{kl} U_{lj}
\end{align*}
$$

or in matrix form 

$$
\begin{equation*}
  \boldsymbol{\varepsilon}' = \mathbf{U}^\dagger \boldsymbol{\varepsilon} \mathbf{U}
\tag{\label{equation-8}}
\end{equation*}
$$

Since $\boldsymbol{\varepsilon}$ is a Hermitian matrix, the spectral theorem guarantees the existence of a unitary matrix $\mathbf{U}$ for which

$$
  \mathbf{U}^\dagger \boldsymbol{\varepsilon} \mathbf{U} = \operatorname{diag}(\varepsilon_1,\dots,\varepsilon_N)
$$

Choosing the occupied orbitals to be the corresponding transformed orbitals under $\mathbf{U}$ therefore diagonalizes the matrix of Lagrange multiplies, reducing the Hartree-Fock equations to the canonical form

$$
  \hat{f}\ket{\chi'_i} = \varepsilon'_i \ket{\chi'_i}
$$

The resulting orbitals from these equations are called the canonical Hartree-Fock orbitals. For non-degenerate orbital energies they are unique up to individual phase factors, whereas degenerate canonical orbitals may still be mixed by arbitrary unitary transformations within each degenerate subspace. More generally, any unitary transformation among the occupied orbitals leaves the Hartree-Fock wavefunction and energy unchanged. We can therefore replace the canonical orbitals by alternative sets satisfying additional criteria, such as maximal spatial localization, without altering any physical observable computed from the Hartree-Fock determinant.

## Koopmans' theorem

For an $N$-electron system, minimization of the Hartree-Fock energy with respect to all normalized Slater determinants $\ket{\Psi_0} = \ket{\chi_1 \cdots \chi_N}$ leads to the Hartree-Fock equations $\hat{f}\ket{\chi_i} = \varepsilon_i \ket{\chi_i}$ for the $N$ occupied spin orbitals $\set{\chi_i}_{i=1^N}$. The Fock operator $\hat{f}$ has a functional dependence on these occupied spin orbitals through the Coulomb and exchange operators. Once the self-consistent solution has been obtained, however, $\hat{f}$ becomes a well-defined Hermitian one-electron operator. Consequently, it possesses a complete orthonormal set of eigenstates satisfying

$$
\begin{equation*}
  \hat{f} \ket{\chi_i} = \varepsilon_i \ket{\chi_i},\; i\in\N
\tag{\label{equation-10}}
\end{equation*}
$$

where the spectrum contains both the $N$ occupied orbitals and an infinite set of unoccupied (virtual) orbitals.

Each eigenstate $\ket{\chi_i}$ is associated with an orbital energy $\varepsilon_i$. By construction, the occupied spin orbitals are those corresponding to the $N$ lowest eigenvalues of the Fock operator, while all remaining eigenstates aare virtual orbitals. In the following we derive explicit expressions for both occupied and virtual orbital energies.

Since the eigenstates of a Hermitian operator form an orthonormal basis, we have

$$
  \braket{\chi_i|\hat{f}|\chi_j} = \varepsilon_j \braket{\chi_i|\chi_j} = \varepsilon_j \delta_{ij}
$$

so that the matrix representation of the Fock operator in its eigenbasis is diagonal.

Using definition $\eqref{equation-5}$ for the Fock operator, the orbital energies can be expressed as

$$
\begin{align*}
  \varepsilon_i =& \braket{\chi_i|\hat{f}|\chi_i} \\
  =& \Braket{\chi_i|\hat{h} + \sum_j (\hat{J}_j - \hat{K}_j)|\chi_i} \\
  =& \braket{\chi_i|\hat{h}|\chi_i} + \sum_j \braket{\chi_i|\hat{J}_j|\chi_i} - \braket{\chi_i|\hat{K}_j|\chi_i} \\
  =& \braket{i|\hat{h}|i} + \sum_j \braket{ij|ij} - \braket{ij|ji} \\
  =& \braket{i|\hat{h}|i} + \sum_j \bra{ij}\ket{ij}
\end{align*}
$$

**Occupied orbital energies**

The orbital energy for an occupied spin orbital $\ket{\chi_a}$ is

$$
\begin{equation*}
  \varepsilon_a = \braket{a|\hat{h}|a} + \sum_{j=1}^N \bra{aj}\ket{aj}
\tag{\label{equation-11}}
\end{equation*}
$$

Since $\bra{aa}\ket{aa} = 0$, we can rewrite this as

$$
\begin{align*}
  \varepsilon_a =& \braket{a|\hat{h}|a} + \sum_{\substack{j=1 \\ j\neq a}}^N \braket{aj|aj} - \braket{aj|ja} \\
  =& h_{aa} + \sum_{\substack{j=1 \\ j\neq a}} J_{aj} - K_{aj}
\end{align*}
$$

In addition to the one-electron contribution $h_{aa}$, the occupied orbital energy $\varepsilon_a$ consists of the Coulomb ($J_{aj}$) and exchange ($K_{aj}$) interactions between the electron occupying $\ke{\chi_a}$ and every other occupied spin orbital.

The exchange integrals $K_{ja}$ vanishes whenever the spin functions of $\ket{\chi_a}$ and $\ket{\chi_j}$ are orthogonal. In this general spin formulation, the exchange term is retained explicitly in the orbital energy expression.

**Virtual orbital energies**

The orbital energy for a virtual spin orbital $\ket{\chi_p}$ is given by

$$
\begin{align*}
  \varepsilon_p =& \braket{p|\hat{h}|p} + \sum_{j=1}^N \bra{pj}\ket{pj} \\
  =& \braket{p|\hat{h}|p} + \sum_j \braket{pj|pj} - \braket{pj|jp}
\end{align*}
$$

Although $\ket{\chi_p}$ is unoccupied in the Hartree-Fock ground state $\ket{\Psi_0}$, its orbital energy $\varepsilon_p$ include Coulomb and exchange interactions will all $N$ occupied electrons. Thus $\varepsilon_p$ can be interpreted as the energy of an additional electron present in the mean field generated by $\ket{\Psi_0}$.

**Relation between orbital energies and the total Hartree-Fock energy**

Summing the occupied orbital energies $\varepsilon_a$ in $\eqref{equation-11}$ for all $N$ electrons in the ground state $\ket{\Psi_0}$ yields

$$
  \sum_a^N \varepsilon_a = \sum_{a=1}^N \braket{a|\hat{h}|a} + \sum_{a, j=1}^N \bra{aj}\ket{aj}
$$

In contrast the Hartree-Fock expectation value $E_0 = \braket{\Psi_0|\hat{H}\Psi_0}$ is given by

$$
  E_0 = \sum_{a=1}^N \braket{a|\hat{h}|a} + \frac{1}{2} \sum_{a,j=1}^N \bra{aj}\ket{aj}
$$

Thus, the total occupied orbital energy does not coincide with the ground state energy

$$
  E_0 \neq \sum_{a=1}^N \varepsilon_a
$$

The discrepancy arises because each pairwise electron-electron interaction appears twice in the sum of orbital energies: once in $\varepsilon_a$ through its interaction with orbital $j$, and again in $\varepsilon_j$ through its interaction with orbital $a$. The factor $1/2$ in the Hartree-Fock energy removes this double counting.

**Physical interpretation of orbital energy**

The physical significance of the orbital energies becomes apparent when we consider the removal or addition of an electron while keeping all remaining spin orbitals fixed.

Consider the $N$-electron Slater determinant $\ket{{}^N \Psi_0} = \ket{\chi_1 \cdots \chi_N}$ with Hartree-Fock energy 

$$
\begin{align*}
  {}^N E_0 =& \braket{{}^N \Psi_0|\hat{H}|{}^N \Psi_0} \\
  =& \sum_{a=1}^N \braket{a|\hat{h}|a} + \frac{1}{2} \sum_{a,b=1}^N \bra{ab}\ket{ab}
\end{align*}
$$

Removing an electron from the occupied spin orbital $\chi_c$ produces the $(N-1)$-electron state

$$
\begin{equation*}
  \ket{{}^{N-1} \Psi_c} = \hat{a}_c \ket{{}^N \Psi_0}
\tag{\label{equation-13}}
\end{equation*}
$$

with Hartree-Fock expectation value ${}^{N-1} E_c = \braket{{}^{N-1} \Psi_c|\hat{H}|{}^{N-1} \Psi_c}$. Assuming that the remaining orbitals are unchanged this becomes

$$
  {}^{N-1} E_c = \sum_{a\neq c} \braket{a|\hat{h}|a} + \frac{1}{2}\sum_{a,b\neq c} \bra{ab}\ket{ab}
$$

The corresponding ionization potential energy for this process is

$$
\begin{align*}
  E_\text{IP} =& {}^{N-1} E_c - {}^N E_0 \\
  =& -\braket{c|\hat{h}|c} - \frac{1}{2} \sum_{a,(b\equiv c)} \bra{ab}\ket{ab} - \frac{1}{2} \sum_{b,(a\equiv c)} \bra{ab}\ket{ab} \\
  =& -\braket{c|\hat{h}|c} - \frac{1}{2} \sum_a \bra{ac}\ket{ac} - \frac{1}{2} \sum_b \bra{cb}\ket{cb} \\
  =& -\braket{c|\hat{h}|c} - \sum_b \bra{cb}\ket{cb} \underbrace{=}_{\eqref{equation-11}} -\varepsilon_c
\end{align*}
$$

Consequently, the ionization potential for removing an electron from the occupied spin orbital $\chi_c$ is just the negative of the orbital energy $\varepsilon_c$.

$$
\begin{equation*}
  E_\text{IP} = {}^{N-1} E_c - {}^N E_0 = -\varepsilon_c
\tag{\label{equation-12}}
\end{equation*}
$$

Thus, within the single-determinant Hartree--Fock approximation, the occupied orbital energies provide estimates of the energies required to remove electrons from their respective spin orbitals, differing only by a minus sign. Since occupied Hartree--Fock orbital energies are typically negative, the corresponding ionization potentials are generally positive.

The validity of $\eqref{equation-12}$ relies on two key approximations
1. **Frozen-orbital approximation:** The orbitals of the remaining $N-1$ electrons are assumed to remain unchanged following ionization, so that the ionized state is represented by the Slater determinant obtained simply by removing the occupied spin orbital $\chi_c$.
2. Electron correlation beyond the Hartree-Fock approximation is neglected. 

In reality, the optimal spin orbitals of $(N-1)$-electron system generally differ from those of the neutral $N$-electron system, and the determinant $\eqref{equation-13}$ need not correspond to the Hartree-Fock ground state of the ionized species. Depending on the choice of the removed spin orbital $\chi_c$, this determinant may represent either the ground state or an excited state of the cation. 

---

Now let us consider the process of adding an electron to a virtual spin orbital $\chi_p$, producing the $(N+1)$-electron Slater determinant 

$$
  \ket{{}^{N+1} \Psi^p} = \hat{a}_p^\dagger \ket{{}^N \Psi_0}
$$

with Hartree-Fock expectation value ${}^{N+1} E_p = \braket{{}^{N+1} \Psi^p|\hat{H}|{}^{N+1} \Psi^p}$. Assuming that the remaining orbitals are unchanged we get

$$
  {}^{N+1} E_p = \braket{p|\hat{h}|p} + \sum_{a=1}^N \braket{i|\hat{h}|i} + \sum_{a=1}^N \bra{pa}\ket{pa} + \frac{1}{2}\sum_{i,j=1}^N \bra{ij}\ket{ij}
$$

The electron affinity of $\ket{{}^N \Psi_0}$ for this process is

$$
\begin{align*}
  E_\text{EA} =& {}^N E_0 - {}^{N+1} E_p \\
  =& -\braket{p|\hat{h}|p} - \sum_b \bra{pb}\ket{pb} \underbrace{=}_{\eqref{equation-11}} -\varepsilon_p
\end{align*}
$$

This shows that the electron affinity for adding an electron to the virtual spin orbital $\chi_p$ is just the negative of the orbital energy of that virtual spin orbital

$$
\begin{equation*}
  E_\text{EA} = {}^N E_0 - {}^{N+1} E_p = -\varepsilon_p
\tag{\label{equation-14}}
\end{equation*}
$$

This result is consistent with the interpretation of $\varepsilon_p$ as the energy of an electron occupying the virtual orbital $\chi_p$, while moving in the mean field generated by the $N$-electron Hartree Fock ground state $\ket{{}^N \Psi_0}$. If $\varepsilon_p < 0$, then the $(N+1)$-electron determinan is lower in energy than the neutral determinan, and the corresponding electron affinity is positive.

As in the ionization process, the exact $(N+1)$-electron Hartree-Fock state generally posesses a different set of self-consisten spin orbitals. Consequently, $\eqref{equation-14}$ is not exact, but follows from the frozen-orbital approximation.

<MathBox title="Koopmans' theorem" boxType="theorem">
Let $\ket{{}^N \Psi_0}$ be the Hartree-Fock ground state Slater determinant of an $N$-electron system, with occupied spin orbitals $\set{\chi_a}$, virutal spin orbitals $\set{\chi_p}$, and corresponding Hartree-Fock orbital energies $\set{\varepsilon_a}$ and $\set{\varepsilon_p}$. Under the frozed-orbital approximation, the ionization potential associated with removing an electron from the occupied spin orbital $\chi_a$

$$
  \ket{{}^{N-1} \Psi_a} = \hat{a}_a \ket{{}^N \Psi_0}
$$

is

$$
  E_\text{IP} = {}^{N-1} E_a - {}^N E_0 = -\varepsilon_a
$$

Likewise, the electron affinity associated with adding an electron to the virtual spin orbital $\chi_p$

$$
  \ket{{}^{N+1} \ket{\Psi}^p} = \hat{a}_p^\dagger \ket{{}^N \Psi_0}
$$

is

$$
  E_\text{EA} = {}^N E_0 - {}^{N+1} E_p = -\varepsilon_p
$$
</MathBox>

Koopmans' theorem provides estimates for ionization potential and electron affinities under the frozen orbital approximation, which assumes that the spin orbitals of the $(N\pm 1)$-electron determinants are identical to those of the $N$-electron Hartree-Fock ground state $\ket{{}^N \Psi_0}$. Thus, the $(N-1)$- and $(N+1)$-electron states are approximated by

$$
\begin{align*}
  \ket{{}^{N-1} \Psi_a} =& \hat{a}_a \ket{{}^N \Psi_0} \\
  \ket{{}^{N+1} \Psi^p} =& \hat{a}_p^\dagger \ket{{}^N \Psi_0}
\end{align*}
$$

without reoptimizing the unchanged spin orbitals. Consequently, the Hartree-Fock orbitals of $\ket{{}^N \Psi_0}$ are generally not the variationally optimal orbitals for either $\ket{{}^{N-1} \Psi_a}$ or $\ket{{}^{N+1} \Psi^p}$.

By the variational principle, separate Hartree-Fock calculations for the $(N\pm 1)$-electron systems, would produce lower energies, i.e.

$$
\begin{align*}
  {}^{N-1} E_a^\text{HF} \leq& {}^{N-1} E_a^\text{FO} \\
  {}^{N+1} E_p^\text{HF} \leq& {}^{N+1} E_p^\text{FO} \\
\end{align*}
$$

where the superscripts "HF" and "FO" denote the fully relaxed and frozen-orbital Hartree-Fock energies, respectively. Orbital relaxation therefore lower the energies of both charged species. This implies that Koopmans' theorem generally overestimates ionization potentials, and underestimates electron affinities.

Beyond orbital relaxation, electron correlation introduces additional corrections that are absent from Hartree--Fock theory. Since the magnitude of the correlation energy generally increases with the number of electrons, the neutral, cationic, and anionic systems receive different correlation corrections. For ionization potentials, these corrections frequently compensate part of the error introduced by neglecting orbital relaxation, leading to a partial cancellation of errors. In contrast, for electron affinities, correlation and relaxation effects often act in the same direction, so that the frozen-orbital Hartree--Fock approximation becomes significantly less accurate.

As a consequence, Koopmans' theorem typically provides reasonable first estimates of experimental ionization potentials but substantially poorer estimates of electron affinities. Indeed, many molecules that are experimentally known to bind an additional electron have positive Hartree--Fock virtual orbital energies, implying a negative electron affinity according to Koopmans' theorem. This failure reflects both the neglect of orbital relaxation and the absence of electron correlation, and illustrates that Hartree--Fock virtual orbital energies generally cannot be interpreted as quantitatively reliable electron attachment energies.

## Brillouin's theorem

The Hartree-Fock equations determine a complete orthonormal set of spin orbitals $\set{\chi_i}_{i=1}^\infty$ consisting of $N$ occupied orbitals $\set{\chi_a}_{a=1}^N$ and an infinite set of virtual orbitals $\set{\chi_p}_{p=1}^\infty$. The Slater determinant $\ket{\Psi_0}$ constructed from the $N$ occupied spin orbitals is the Hatree-Fock approximation to the electonic ground state.

The Hartree-Fock orbital basis also generates an infinite collection of excited Slater determinants. Of particular interest are the singly excited determinants

$$
  \ket{\Psi_a^p} = \hat{a}_p^\dagger \hat{a}_a \ket{\Psi_0}
$$

obtained by replacing an occupied spin orbital $\chi_a$ with a virtual spin orbital $\chi_p$. Intuitively, we expect that the leading correction to the exact electronic ground state $\ket{\Phi_0}$ would arise from a linear combination of singly excited determinants

$$
  \ket{\Phi_0} = c_0 \ket{\Psi_0} + \sum_{p,a} c_a^p \ket{\Psi_a^p}
$$

If the configuration-interaction expansion is truncated to the Hartree-Fock reference determinant and all singly excited determinants, the coefficients are obtained by applying the Rayleigh-Ritz variational principle, leading to the matrix eigenvalue problem $\mathbf{Hc} = E\mathbf{c}$, where the Hamiltonian matrix $\mathbf{H}$ is represented in the basis $\set{\ket{\Psi_0}, \ket{\Psi_a^p}}$.

Consider the subspace spanned by the Hartee-Fock reference determinant and a single excited determinant. The corresponding equation is

$$
\begin{equation*}
  \begin{bmatrix}
    \braket{\Psi_0|\hat{H}|\Psi_0} & \braket{\Psi_0|\hat{H}|\Psi_a^p} \\
    \braket{\Psi_a^p|\hat{H}|\Psi_0} & \braket{\Psi_a^p|\hat{H}|\Psi_a^p}
  \end{bmatrix}
  \begin{bmatrix} c_0 \\ c_a^p \end{bmatrix} = E_0 \begin{bmatrix} c_0 \\ c_a^p \end{bmatrix}
\tag{\label{equation-15}}
\end{equation*}
$$

The extent to which these two determinants mix is governed by the off-diagonal Hamiltonian matrix elements. By the Slater-Condon rules

$$
\begin{align*}
  \braket{\Psi_0|\hat{H}|\Psi_a^p} =& \braket{a|\hat{h}|p} + \sum_j \bra{aj|pj} \\
  =& \braket{\chi_a|\hat{f}|\chi_p}
\end{align*}
$$

which is precisely the off-diagonal matrix element of the Fock operator $\hat{f}$ in the Hatree-Fock orbital basis. Since $\hat{f}$ is Hermitian, its eigenstates form a complete orthonormal basis. The spin-orbital satisfying the Hartree-Fock equation $\eqref{equation-6}$ therefore diagonalize $\hat{f}$, so that $\braket{\chi_i|\hat{f}|\chi_j} = \varepsilon_j \delta_{ij}$. This implies in particular that all singly excited determiants vanish

$$
  \braket{\Psi_0|\hat{H}|\Psi_a^p} = 0
$$

This result is known as Brillouin's theorem. It states that the Hartree--Fock reference determinant is orthogonal, with respect to the Hamiltonian, to every singly excited determinant constructed from its canonical Hartree--Fock spin orbitals. Equivalently, the Hartree--Fock energy is stationary with respect to all single excitations.

The matrix equation $\eqref{equation-15}$ therefore reduces to

$$
  \begin{bmatrix}
    E_0 & 0 \\
    0 & \braket{\Psi_a^p|\hat{H}|\Psi_a^p}
  \end{bmatrix}
  \begin{bmatrix} 1 \\ 0 \end{bmatrix} = E_0 \begin{bmatrix} 1 \\ 0 \end{bmatrix}
$$

showing that the Hartree-Fock determinant is already stationary with respect to all single excitations. Equivalently, the Hatree-Fock energy cannot be lowered by variational mixing with singly determinants alone.

It follows that the leading correction to the Hartree-Fock wavefunction arise from doubly excited determinants

$$
  \ket{\Psi_{ab}^{pq}} = \hat{a}_p^\dagger \hat{a}_q^\dagger \hat{a}_b \hat{a}_a \ket{\Psi_0}
$$

since these have in general $\braket{\Psi_0|\hat{H}|\Psi_{ab}^{pq}} \neq 0$. This does not, however, imply that singly excited determinants are absent from the exact ground-state wavefunction. Although they do not couple directly to the Hartree-Fock reference determinant, they may acquire nonzero amplitudes through indirect coupling to higher excitations. For example, the Hamiltonian matrix elements $\braket{\Psi_0|\hat{H}|\Psi_{ab}^{pq}}$ and $\braket{\Psi_{ab}^{pq}|\hat{H}|\Psi_a^p}$ allow singly excited determinants to mix with the Hartree-Fock reference $\ket{\Psi_0}$ determinant through doubly excited configurations. Consequently, the exact full configuration-interaction ground state generally contains contributions from determinants of all excitation ranks, even though the first-order correction to the Hartree-Fock wavefunction contains no singly excited determinants.

## Møller-Plesset Hamiltonian

Let $\set{\chi_i}_{i=1}^\infty$ be a complete orthonormal basis of spin orbitals satisfying th Hartree-Fock equation $\eqref{equation-6}$. From these orbitals we define the Møller-Plesset zeroth-order Hamiltonian

$$
  \hat{H}_0 = \sum_{i=1}^N \hat{f}(i)
$$

where $\hat{f}(i)$ is Fock operator for the $i$th electron. Since each $\hat{f}(i)$ is a one-electron operators, it commutes with every permutation operator

$$
  [\hat{f}(i), \hat{P}] = 0,\; P \in S_N
$$

and therefore so does $\hat{H}_0$. Letting $\hat{H}_0$ act on the Hartree-Fock Slater determinant

$$
  \jet{\Psi_0} = \frac{1}{\sqrt{N!}} \sum_{P\in S_N} (-1)^P \hat{P} [\chi_1 (1) \cdots \chi_N (N)]
$$

we obtain

$$
\begin{align*}
  \hat{H}_0 \ket{\Psi_0} =& \frac{1}{\sqrt{N!}} \sum_{P\in S_N} (-1)^P \hat{P} \left(\sum_{i=1}^N \hat{f}(i) \right) [\chi_1 \cdots \chi_N (N)] \\
  =& \frac{1}{\sqrt{N!}} \sum_{P\in S_N} (-1)^P \hat{P} \left(\sum_{i=1}^N \varepsilon_i \right) [\chi_1 \cdots \chi_N (N)] \\
  =& \left(\sum_{i=1}^N \varepsilon_i \right) \ket{\Psi_0}
\end{align*}
$$

The the Hartree-Fock reference determinant is an eigenstate of the zero-order Hamiltonian with eigenvalue

$$
  E_0^{(0)} = \sum_{i=1}^N \varepsilon_i
$$

More generally, every Slater determinant constructed from the canonical Hartree-Fock spin orbitals is an eigenstate of $\hat{H}_0$ with eigenvalue equal to the sum of the orbital energies of its occupied spin orbitals. Since these determinants form a complete orthonormal basis of the antisymmetric $N$-electron Hilbert space, they constitute a complete eigenbasis of $\hat{H}_0$. This makes $\hat{H}_0$ a natural choice for the unperturbed Hamiltonian in Rayleigh-Schrödinger perturbation theory.

The exact Hamiltonian can be decomposed as $\hat{H} = \hat{H}_0 + \hat{V}$, where $\hat{H}_0$ is an unperturbed Hamiltonian, and $\hat{V}$ is a perturbation operator given by

$$
\begin{align*}
  \hat{V} =& \hat{H} - \hat{H}_0 \\
  =& \sum_{i=1}^N \hat{h}(i) + \sum_{i=1}^N \sum_{j>i}^N \frac{1}{r_{ij}} - \sum_{i=1}^N \hat{f}(i) \\
  \underbrace{=}_{\eqref{equation-16}}& \sum_{i=1}^N \sum_{j>i}^N \frac{1}{r_{ij}} - \sum_{i=1}^N \hat{v}^\text{HF} (i)
\end{align*}
$$

Thus, the perturbation is simply the difference between the Coulomb interaction and the Hartree-Fock mean-field potential.

The expectation value of $\hat{H}$ with respect to the Hartree-Fock reference determinant is

$$
\begin{equation*}
\begin{split}
  \braket{\Psi_0|\hat{H}|\Psi_0} =& \braket{\Psi_0|\hat{H}_0|\Psi_0} + \braket{\Psi_0|\hat{V}|\Psi_0} \\
  =& \sum_{a=1}^N \varepsilon_a + \braket{\Psi_0|\hat{V}|\Psi_0}
\end{split}
\tag{\label{equation-17}}
\end{equation*}
$$

The expectation value of the perturbation is

$$
  \braket{\Psi_0|\hat{V}|\Psi_0} = \Braket{\Psi_0| \sum_{i < j}\frac{1}{r_{ij}}|\Psi_0} - \Braket{\Psi_0|\sum_{i=1}^N \hat{v}^\text{HF} |\Psi_0} \\
$$

Using the Slater-Condon rules, the first term is

$$
  \Braket{\Psi_0| \sum_{i < j}\frac{1}{r_{ij}}|\Psi_0} = \frac{1}{2} \sum_{a,b}^N \bra{ab}\ket{ab}
$$

while the second term is

$$
\begin{align*}
  \Braket{\Psi_0|\sum_{i=1}^N \hat{v}^\text{HF}|\Psi_0} =& \sum_{a=1}^N \Braket{a|\sum_{b=1}^N (\hat{J}_j - \hat{K}_j)|a} \\
  =& \sum_{a,b=1}^N \bra{ab}\ket{ab}
\end{align*}
$$

Combining the terms gives

$$
\begin{align*}
  \braket{\Psi_0|\hat{V}|\Psi_0} =& \frac{1}{2} \sum_{a,b=1}^N \bra{ab}\ket{ab} - \sum_{a,b=1}^N \bra{ab}\ket{ab} \\
  =& -\frac{1}{2} \sum_{a,b=1}^N \bra{ab}\ket{ab}
\end{align*}
$$

Substituting this into $\eqref{equation-17}$, we obtain

$$
\begin{align*}
  \braket{\Psi_0|\hat{H}|\Psi_0} =& \sum_{a=1}^N \varepsilon_a - \frac{1}{2} \sum_{a,b=1}^N \bra{ab}\ket{ab} \\
  =& \sum_{a=1} \braket{a|\hat{h}|a} + \frac{1}{2} \sum_{a,b=1}^N \bra{ab}\ket{ab}
\end{align*}
$$

which is precisely the Hartree-Fock ground state energy $E_0^\text{HF} = \braket{\Psi_0|\hat{H}|\Psi_0}$. Within Rayleigh-Schrödinger perturbation theory

$$
\begin{align*}
  E_0^{(0)} :=& \braket{\Psi_0|\hat{H}_0|\Psi_0} \\
  E_0^{(1)} :=& \braket{\Psi_0|\hat{V}|\Psi_0}
$$

so that the Hartree-Fock energy is decomposed as

$$
  E_0^\text{HF} = E_0^{(0)} + E_0^{(1)}
$$

The exact ground-state energy admits the perturbation expansion

$$
\begin{align*}
  E_0 =& E_0^{(0)} + E_0^{(1)} + E_0^{(2)} \cdots = E_0^\text{HF} + \sum_{n=2}^\infty E_0^{(n)}
  =& E_0^\text{HF} + E_0^{(2)} + \cdots
\end{align*}
$$

The second- and higher-order corrections arise from the perturbative corrections to the wavefunction and account for the electron correlation missing from the Hartree--Fock approximation.

# Restriced closed-shell Hartree-Fock

A restricted Hartree-Fock (RHF) wavefunction is constructed from spin orbitals that have the form

$$
  \chi_i (\mathbf{x}) = \begin{cases}
    \psi_j (\mathbf{r}) \alpha(\omega) \\
    \psi_j (\mathbf{r}) \beta(\omega)
  \end{cases},\quad a = 1,\dots,N/2
$$

where $\mathbf{x} = (\mathbf{r}, \omega)$ denotes the combined spatial and spin coordinate.

In the closed-shell restricted Hartree-Fock ground state, every occupied spatial orbital is doubly occupied by one $\alpha$-spin and one $\beta$-spin electron

$$
\begin{align*}
  \ket{\Psi_0} =& \ket{\chi_1 \chi_2 \cdots \chi_{N-1} \chi_N} \\
  =& \ket{\psi_1 \bar{\psi}_1 \cdots \psi_{N/2} \bar{\psi}_{N/2}}
\end{align*}
$$

where $\psi_a := \psi_a \alpha$ and $\bar{\psi}_a = \psi_a \beta$ denotes spin orbitals with $\alpha$ and $\beta$ spin functions, respectively.

Our goal is to reduce the closed-shell Hartree-Fock equation

$$
\begin{equation*}
  \hat{f}(\mathbf{x}_1) \chi_i (\mathbf{x}_1) = \varepsilon_i \chi_i (\mathbf{x}_1)
\tag{\label{equation-19}}
\end{equation*}
$$

to an eigenvalue involving only the spatial orbitals.

Suppose $\chi_i$ and an $\alpha$-spin orbital (the derivation for a $\beta$-spin orbital is identical)

$$
  \chi_i (\mathbf{x}_1) = \psi_j (\mathbf{r}) \alpha(\omega)
$$ 

In this case, the Hartree-Fock equation $\eqref{equation-19}$ become

$$
  \hat{f}(\mathbf{x}_1) \psi_i (\mathbf{r}_1) \alpha(\omega_1) = \varepsilon_i \psi_i (\mathbf{r}_1) \alpha(\omega_1)
$$

Since the two spin orbitals associated with the same spatial orbital are degenerate, the corresponidng spatial orbital has the same orbital energy, i.e. $\varepsilon_{\psi_i} = \varepsilon_{\bar{\psi}_i}$.

The spatial orbital energy $\varepsilon_j$ is identical to the spin orbital energy, because the two spin orbitals associated with the same spatial orbital are degenerate.

Multiplying on the left by $\alpha^* (\omega_1)$ and integrating over the spin coordinate yields

$$
\begin{align*}
  \left(\int \d\omega_1 \alpha^* (\omega_1) \hat{f}(\mathbf{x}_1) \alpha(\omega_1) \right)\psi_j (\mathbf{r}_1) =& \varepsilon_j \psi_j (\mathbf{r}_1) \underbrace{\int \d\omega_1 \alpha^* (\omega_1) \alpha (\omega_1)}_{=\braket{\alpha|\alpha} = 1} \\
  =& \varepsilon_j \psi_j (\mathbf{r}_1)
\end{align*}
$$

Using the form $\eqref{equation-18}$ for the Fock operator, the left-hand side becomes

$$
\begin{align*}
  & \left(\int \d\omega_1 \alpha^* (\omega_1) \hat{f}(\mathbf{x}_1) \alpha(\omega_1) \right)\psi_j (\mathbf{r}_1) \\
  =& \left(\int \d\omega_1 \alpha^* (\omega_1) \hat{h}(\mathbf{x}_1) \alpha(\omega_1) \right)\psi_j (\mathbf{r}_1) \\
  &+ \left(\sum_c \int \d\omega_1 \;\d\mathbf{x}_2  \alpha^* (\omega_1) \chi_c^* (\mathbf{x}_2) \frac{1}{r_{12}} \chi_c^* (\mathbf{x}_2) \frac{1}{r_{12}} (1 - \hat{p}_{12}) \chi_c (\mathbf{x}_2) \alpha(\omega_1) \right) \psi_j (\mathbf{r}_j) \\
  =& \varepsilon_j \psi_j (\mathbf{r}_1)
\end{align*}
$$

Since the one-electron operator $\hat{h}(\mathbf{r}_1)$ is independent of spin, the first integral term is

$$
\begin{align*}
  \int \d\omega_1 \alpha^* (\omega_1) \hat{h}(\mathbf{r}_1) \alpha(\omega_1) =& \hat{h}(\mathbf{r}_1) \underbrace{\int \d\omega_1 |\alpha(\omega_1)|^2}_{=1} \\
  =& \hat{h}(\mathbf{r}_1)
\end{align*}
$$

Letting $\hat{f}(\mathbf{r}_1)$ be the closed-shell Fock operator

$$
  \hat{f}(\mathbf{r}_1) = \int \d\omega_1 \alpha^* (\omega_1) \hat{f}(\mathbf{x}_1) \alpha(\omega_1)
$$

then

$$
\begin{align*}
  \hat{f}(\mathbf{r}_1) \psi_j (\mathbf{r}_1) =& \hat{h}(\mathbf{r}_1) \psi_j (\mathbf{r}_1) \\
  &+ \sum_c \int \d\omega_1 \;\d\mathbf{x}_2 \alpha^* (\omega_1) \chi_c^* (\mathbf{x}_2) \frac{1}{r_{12}} \chi_c (\mathbf{x}_2) \alpha(\omega_1) \psi_j (\mathbf{r}_1) \\
  &- \sum_c \int \d\omega_1 \;\d\mathbf{x}_2 \alpha^* (\omega_1) \chi_c^* (\mathbf{x}_2) \frac{1}{r_{12}} \chi_c (\mathbf{x}_1) \alpha(\omega_2) \psi_j (\mathbf{r}_2) \\
  =& \varepsilon_j \psi_j (\mathbf{r}_1)
\end{align*}
$$

In a closed-shell determinant, every occupied spatial orbital contributes one $\alpha$- and one $\beta$-spin electron, so that that the sum over spin is partitioned unto

$$
  \sum_c^N \chi_c = \sum_c^{N/2} \psi_c \alpha + \sum_c^{N/2} \psi_c \beta
$$

Using orthonormality of the spin functions, we get

$$
\begin{align*}
  \hat{f}(\mathbf{r}_1) \psi_j (\mathbf{r}_1) =& \hat{h}(\mathbf{r}_1) \psi_j (\mathbf{r}_1) \\
  &+ \sum_c^{N/2} \int \;\d\mathbf{r}_2 \psi_c^* (\mathbf{r}_2) \frac{1}{r_{12}} \psi_c (\mathbf{r}_2) \psi_j (\mathbf{r}_1) \underbrace{\int \d\omega_1 |\alpha(\omega_1)|^2}_{=1} \underbrace{\int \d\omega_2 |\alpha(\omega_2)|^2}_{=1} \\
  &+ \sum_c^{N/2} \int \;\d\mathbf{r}_2 \psi_c^* (\mathbf{r}_2) \frac{1}{r_{12}} \psi_c (\mathbf{r}_2) \psi_j (\mathbf{r}_1) \underbrace{\int \d\omega_1 |\alpha(\omega_1)|^2}_{=1} \underbrace{\int \d\omega_2 |\beta(\omega_2)|^2}_{=1} \\
  &- \sum_c^{N/2} \int \;\d\mathbf{r}_2 \psi_c^* (\mathbf{r}_2) \frac{1}{r_{12}} \psi_c (\mathbf{r}_2) \psi_j (\mathbf{r}_1) \underbrace{\int \d\omega_1 |\alpha(\omega_1)|^2}_{=1} \underbrace{\int \d\omega_2 |\alpha(\omega_2)|^2}_{=1}\\
  &- \sum_c^{N/2} \int \;\d\mathbf{r}_2 \psi_c^* (\mathbf{r}_2) \frac{1}{r_{12}} \psi_c (\mathbf{r}_2) \psi_j (\mathbf{r}_1) \underbrace{\int \d\omega_1 \alpha^* (\omega_1) \beta(\omega_1)}_{=0} \underbrace{\int \d\omega_2 \beta^*(\omega_2) \alpha(\omega_2)}_{=0}\\
  =& \varepsilon_j \psi_j (\mathbf{r}_1)
\end{align*}
$$

We can now perform the integrations over $\d\omega_1$ and $\d\omega_2$. The last term disappears because of spin orthogonality, $\braket{\alpha|\beta} = 0$. This reflects the fact that there is only an exchange interaction between electrons of parallel spin. The two Coulomb terms are equal and thus we obtain

$$
\begin{align*}
  \hat{f}(\mathbf{r}_1) \psi_j (\mathbf{r}_1) =& \hat{h}(\mathbf{r}_1) \psi_j (\mathbf{r}_1) \\
  &+ \left(2 \sum_{c}^{N/2} \int \d\mathbf{r}_2 \psi_c^* (\mathbf{r}_2) \frac{1}{r_{12}} \psi_c (\mathbf{r}_2) \right) \psi_j (\mathbf{r}_1) \\
  &+ \left(\sum_{c}^{N/2} \int \d\mathbf{r}_2 \psi_c^* (\mathbf{r}_2) \frac{1}{r_{12}} \psi_c (\mathbf{r}_2) \right) \psi_c (\mathbf{r}_1) \\
  =& \varepsilon_j \psi_j (\mathbf{r}_1)
\end{align*}
$$

The closed-shell Fock operator therefore has the form

$$
  \hat{f}(\mathbf{r}_1) = \hat{h}(\mathbf{r}_1) + \sum_a^{N/2} \int \d\mathbf{r}_2 \psi_a^* (\mathbf{r}_2) (2 - \hat{p}_{12}) \frac{1}{r_{12}} \psi_a (\mathbf{r}_2)
$$

or, equivalently

$$
  \hat{f}(1) = \hat{h}(1) + \sum_a^{N/2} 2J_a (1) - K_a (1)
$$

where the closed-shell Coulomb and exchange operators are defined by

$$
\begin{align*}
  \hat{J}_a (1) =& \int \d\mathbf{r}_2 \psi_a^* (2) \frac{1}{r_{12}} \psi_a (2) \\
  K_a (1) \psi_i (1) =& \left(\int \d\mathbf{r}_2 \psi_a^* (2) \frac{1}{r_{12}} \psi_i (2) \right) \psi_a (1)
\end{align*}
$$

By the Slater-Condon rules for spatial orbitals, the closed-shell Hartree-Fock energy $E_0^\text{HF} = \braket{\Psi_0|\hat{H}|\Psi_0}$ is given by

$$
\begin{align*}
  E_0 =& 2 \sum_{a=1}^N (a|\hat{h}|a) + \sum_{a,b=1}^N 2(aa|bb) - (ab|ba) \\
  =& 2\sum_{a=1}^N h_{aa} + \sum_{a,b=1}^N 2J_{ab} - K_{ab}
\end{align*}
$$

and the orbital energy is

$$
\begin{align*}
  \varepsilon_i =& (\psi_i|\hat{h}|\psi_i) + \sum_{b=1}^{N/2} 2(ii|bb) - (ib|bi) \\
  =& h_{ii} + \sum_{b=1}^{N/2} 2J_{ij} - K_{ib}
\end{align*}
$$

## Roothan equations

By introducing a finite set of known spatial basis functinos, the spatial Hartree-Fock equations

$$
\begin{equation*}
  \hat{f}(\mathbf{r}_1) \psi_i (\mathbf{r}_1) = \varepsilon_i \psi_i (\mathbf{r}_i)
\tag{\label{equation-21}}
\end{equation*}
$$

can be converted into a finite-dimensional matrix eigenvalue problem. Let $\set{\phi_\mu (\mathbf{r})}_{\mu=1}^K$ be a finite set of linearly independent basis functions spanning a $K$-dimensional subspace of the one-electron Hilbert space. Each molecular orbital is approximated by the linear combination

$$
\begin{equation*}
  \psi_i = \sum_{\mu=1}^K C_{\mu i} \phi_i,\, i=1,\dots,K
\tag{\label{equation-20}}
\end{equation*}
$$

where the coefficients $C_{\mu i}$ are unknown.

If the basis were complete $(K\to\infty)$, this expansion could represent any square-integrable orbital exactly. In practice, however, only a finite basis can be used, so the expansion is restricted to the subspace spanned by $\set{\phi_\mu}$. Thus, the Hartree-Fock orbitals obtained from $\eqref{equation-20}$ are the best approximation within this subspace. In complete basis set limit, the approximate molecular orbitals converge to the exact Hartree-Fock orbitals.

We can obtain a matrix equation for $C_{\mu i}$ by substituting the linear expansion $\eqref{equation-20}$ in to the Hartree-Fock equation $\eqref{equation-21}$

$$
  \hat{f}(1) \sum_{\nu=1}^K C_{\nu,i} \phi_\nu (1) = \varepsilon_i \sum_{\nu=1}^K C_{\nu i} \phi_\nu (1)
$$

By multiplying by $\phi_\mu^* (1)$ on the left and integrating, we turn the integro-differential equation into a matrix equation

$$
\begin{equation*}
  \sum_{\nu=1}^K C_{\nu i} \int \d\mathbf{r}_1 \; \phi_\mu^* (1) \hat{f}(1) \phi_\nu (1) = \varepsilon_i \sum_{\nu=1}^K C_{\nu,i} \int \d\mathbf{r}_1 \; \phi_\nu^* (1) \phi_\nu (1)
\tag{\label{equation-22}}
\end{equation*}
$$

To simplify the notation, we define two matrices:
1. The overlap matrix $\mathbf{S} \in\mathcal{M}_K (\mathbb{C})}$ has elements
$$
\begin{equation*}
  S_{\mu\nu} = \braket{\phi_\mu|\phi_\nu} = \int \d\mathbf{r} \; \phi_\mu^* (\mathbf{r}) \phi_\nu (\mathbf{r})
\tag{\label{equation-23}}
\end{equation*}
$$

and is Hermitian, satisfying $S_{\mu\nu} = S_{\nu\mu}^*$. Provided the basis functions are linearly independent, it is positive definite. If the basis functions are individually normalized, the diagonal elements satisfy $S_{\mu\mu} = 1$, while the off-diagonal elements satisfy $|S_{\mu\nu}| < 1$ for $\mu\neq\nu$.

Near-linear dependence among the basis functions is reflected by one or more eigenvalues of $\mathbf{S}$ approaching zero. Since $\mathbf{S}$ defines the inner product in the finite basis representation, it is often referred to as the metric matrix.

2. The Fock matrix $\mathbf{F} \in\mathcal{M}_K (\mathbb{C})$ has elements
$$
\begin{equation*}
  F_{\mu\nu} = \braket{\phi_\mu|\hat{f}|\phi_\nu} = \int \d\mathbf{r} \; \phi_\mu^* (\mathbf{r}) \hat{f}(\mathbf{r}) \phi_\mu (\mathbf{r})
\tag{\label{equation-24}}
\end{equation*}
$$

and is simply the matrix representation of the Fock operator in the chosen basis. Because $\hat{f}$ is Hermition, it follows that $\mathbf{F}$ is Hermitian, i.e. $F_{\mu\nu} = F_{\nu\mu}^*$.

With the matrix definitions $\eqref{equation-23}$ and $\eqref{equation-24}$ the integrated Hatree-Fock equation $\eqref{equation-22}$ takes the form

$$
  \sum_{\nu=1}^N F_{\mu\nu} C_{\mu i} = \varepsilon_i \sum_{\nu=1}^N S_{\mu\nu} C_{\nu i},\, i=1,\dots,K
$$

These are the Roothan equations, which can be written more compactly as the single matrix equation

$$
  \mathbf{FC} = \mathbf{SC}\boldsymbol{\varepsilon}
$$

where $\mathbf{C}$ is a $K\times K$ square matrix of the expansion coefficients $C_{\mu i}$ and $\boldsymbol{\varepsilon} = \operatorname{diag}(\varepsilon_1,\dots,\varepsilon_K)$ is a diagonal matrix of the orbital energies.

This is a generalized Hermitian eigenvalue problem because the basis functions are generally nonorthogonal, i.e. $\mathbf{S} \neq \mathbf{I}_K$. If the basis is orthonormal, $\mathbf{S} = \mathbf{I}_K$, the equations reduce to the ordinary eigenvalue problem

$$
  \mathbf{FC} = \mathbf{C}\boldsymbol{\varepsilon}
$$

## Charge density

Consider an electron described by a spatial wavefunction $\psi_a (\mathbf{r})$. The probability of finding that electron within a volume element $\d\mathbf{r}$ is $|\psi_a (\mathbf{r})|^2 \;\d\mathbf{r}$ such that the charge density is $|\psi_a (\mathbf{r})|^2$. In a closed-shell molecule described by a Slater determinant with each occupied spatial orbital $\psi_a$ contains two electrons, the total charge density is

$$
\begin{equation*}
  \rho(\mathbf{r}) = 2 \sum_{a=1}^{N/2} |\psi_a (\mathbf{r})|^2
\tag{\label{equation-25}}
\end{equation*}
$$

such that $\rho(\mathbf{r})\;\d\mathbf{r}$ is the probability of finding an electron in $\d\mathbf{r}$ at $\mathbf{r}$. The integral of this charge density is just the total number of electrons

$$
\begin{align*}
  \int \d\mathbf{r}\; \rho(\mathbf{r}) =& 2 \sum_{a=1}^{N/2} \underbrace{\int \d\mathbf{r}\; |\psi_a (\mathbf{r})|^2}_{=1} \\
  =& 2 \sum_{a=1}^{N/2} 1 = N
\end{align*}
$$

For a single determinant, these equations show that the total charge density is just a sum of cherge densities for each of the electrons.

Substituting the molecular orbital expansion $\eqref{equation-20}$ into $\eqref{equation-25}$, we get

$$
\begin{align*}
  \rho(\mathbf{r}) =& 2\sum_{a=1}^{N/2} \psi_a^* (\mathbf{r}) \psi_a (\mathbf{r}) \\
  =& 2 \sum_{a=1}^{N/2} \sum_{\nu=1}^{N/2} C_{\nu a}^* \phi_\nu^* (\mathbf{r}) \sum_{\mu=1}^{N/2} C_{\mu a} \phi_\mu (\mathbf{r}) \\
  =& \sum_{\mu,\nu=1}^{N/2} \left(2 \sum_{a=1}^{N/2} C_{\mu a} C_{\nu a}^* \right) \phi_\mu (\mathbf{r}) \phi_\nu^* (\mathbf{r}) \\
  =& \sum_{\mu,\nu=1}^{N/2} P_{\mu\nu} \phi_\mu (\mathbf{r}) \phi_\nu^* (\mathbf{r})
\end{align*}
$$

where we have defined a density matrix, also called charge-density bond-order matrix, $\mathbf{P}$ with elements

$$
  P_{\mu\nu} = 2 \sum_{a=1}^{N/2} C_{\mu a} C_{\nu a}^*
$$

Given a set of known basis functions $\set{\phi_\mu}$, the matrix $\mathbf{P}$ specifies completely the charge density $\rho(\mathbf{r})$. It is direcltl related to the expansion coefficients $\mathbf{C}$ and we can characterized the results of closed-shell Hartree-Fock calculations by either $\mathbf{C}$ or $\mathbf{P}$.

In terms of $\mathbf{P}$, the Fock operator can be expressed as

$$
  \hat{f}(\mathbf{r}_1) = \hat{h}(\mathbf{r}_1) + \frac{1}{2} \sum_{\lambda,\sigma} P_{\lambda,\sigma} \left(\int \;\d\mathbf{r}_2 \; \phi_a^* (\mathbf{r}_2) (2 - \hat{P}_{12}) \frac{1}{r_{12}} \phi_\lambda (\mathbf{r}_2) \right)
$$


# Unrestricted open-shell Hartree-Fock
